# =============================================================================
# Database Configuration
# =============================================================================
# PostgreSQL connection string (Neon or local PostgreSQL)
# Format: postgresql://user:password@host:port/database
# Example for Neon: postgresql://user:password@ep-xxx-xxx.us-east-2.aws.neon.tech/neondb
DATABASE_URL=postgresql://user:password@localhost:5432/todo_db

# =============================================================================
# Authentication & Security
# =============================================================================
# Shared secret for JWT token generation and verification
# MUST match the BETTER_AUTH_SECRET in frontend .env.local
# Generate a secure secret: openssl rand -base64 32
# Minimum 32 characters recommended
BETTER_AUTH_SECRET=your-secret-key-here-minimum-32-characters-long

# JWT token expiration in days (default: 7)
JWT_EXPIRATION_DAYS=7

# =============================================================================
# Application Configuration
# =============================================================================
# Environment: development, production, test
ENVIRONMENT=development

# Server port (default: 8000)
PORT=8000

# Request timeout in seconds (default: 30)
REQUEST_TIMEOUT=30

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL (default: INFO)
LOG_LEVEL=INFO

# =============================================================================
# CORS Configuration
# =============================================================================
# Allowed CORS origins (comma-separated)
# Default: http://localhost:3000
# For production, add your frontend domain(s)
CORS_ORIGINS=http://localhost:3000

# =============================================================================
# AI Provider Configuration (Phase III)
# =============================================================================
# LLM Provider: openai or gemini
LLM_PROVIDER=openai

# OpenAI Configuration (required if LLM_PROVIDER=openai)
# Get API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_DEFAULT_MODEL=gpt-4o

# Gemini Configuration (required if LLM_PROVIDER=gemini)
# Get API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_DEFAULT_MODEL=gemini-1.5-flash

# =============================================================================
# Optional: Monitoring & Observability
# =============================================================================
# Uncomment if using external monitoring services
# SENTRY_DSN=your-sentry-dsn-here
# DATADOG_API_KEY=your-datadog-api-key-here