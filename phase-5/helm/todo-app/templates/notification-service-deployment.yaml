apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.notificationService.name }}
  labels:
    app: {{ .Values.notificationService.name }}
    version: {{ .Chart.AppVersion }}
spec:
  replicas: {{ .Values.notificationService.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.notificationService.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.notificationService.name }}
        version: {{ .Chart.AppVersion }}
      annotations:
        dapr.io/enabled: "{{ .Values.notificationService.dapr.enabled }}"
        dapr.io/app-id: "{{ .Values.notificationService.dapr.appId }}"
        dapr.io/app-port: "{{ .Values.notificationService.dapr.appPort }}"
        dapr.io/config: "{{ .Values.notificationService.dapr.config }}"
        dapr.io/log-level: "{{ .Values.notificationService.dapr.logLevel }}"
        dapr.io/log-as-json: "true"
    spec:
      containers:
        - name: {{ .Values.notificationService.name }}
          image: "{{ .Values.notificationService.image.repository }}:{{ .Values.notificationService.image.tag }}"
          imagePullPolicy: {{ .Values.notificationService.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.notificationService.port }}
              protocol: TCP
          env:
            {{- range .Values.notificationService.env }}
            - name: {{ .name }}
              {{- if .value }}
              value: {{ .value | quote }}
              {{- else if .valueFrom }}
              valueFrom:
                {{- toYaml .valueFrom | nindent 16 }}
              {{- end }}
            {{- end }}
          resources:
            {{- toYaml .Values.notificationService.resources | nindent 12 }}
          {{- if .Values.healthChecks.enabled }}
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: {{ .Values.healthChecks.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.healthChecks.livenessProbe.periodSeconds }}
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: {{ .Values.healthChecks.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.healthChecks.readinessProbe.periodSeconds }}
          {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.notificationService.name }}
  labels:
    app: {{ .Values.notificationService.name }}
spec:
  type: {{ .Values.notificationService.service.type }}
  ports:
    - port: {{ .Values.notificationService.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.notificationService.name }}
